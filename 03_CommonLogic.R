
# dataframe is used through the whole programme 
df_ind <- data.frame(code = c("Ag", "Fo", "Fi", "Mi"), 
                     name = c("Agriculture, Forestry", "Fisheries", "Mining")) 
df_currency <- data.frame(code = c("us", "canada", "vietnam"), 
                          name = c("USD", "CAD", "vnd")) 

# statistics on industry variable
stat_on_industry <- function(df){
} 

# statistics on currency variable  
stat_on_currency <- function(df){
} 

# calculate profit
generate_profit <- function(df){   
  
} 

