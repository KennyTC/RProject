
# set themes 
theme1 <- gridExtra::ttheme_default(core = list(fg_params = list(cex = 1.5)), 
                                    colhead = list(fg_params = list(cex = 1.5)), 
                                    rowhead = list(fg_params = list(cex = 1.5))) 
theme2 <- gridExtra::ttheme_default(core = list(fg_params = list(cex = 0.3)), 
                                    colhead = list(fg_params = list(cex = 1)), 
                                    rowhead = list(fg_params = list(cex = 1))) 

# plot importance feature from train data 
plot_importancefeature <- function(df) {  
  
} 

# plot currency vs target   
plot_currency_vs_y <- function(df){ 
  
  
  
} 


